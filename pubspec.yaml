name: projeto_estagio
description: "Projeto em Flutter"

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ">=2.19.0 <3.0.0"  # Use uma versão compatível

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2  # Use a versão mais recente
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/imagemfundo.jpg 
